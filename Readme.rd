=========CONSIDERACIONES==========

1. Modificar la configuracion de paquetes que se desee obtener en el archivo config/config.php
2. Modificar la cadena de conexion a base de datos.
3. Crear la carpeta 'generated' en la raiz del proyectos.
4. Usar sólo PK simples.
5. Todas las tablas, sin excepción deben contemplar un campo PK.
6. Los campos con valores numéricos deben manejar el prefijo 'N_'.
7. Los campos con valores alfanuméricos deben manejar el prefijo 'C_'.
8. Los campos con valores del tipo fecha deben manejar el prefijo 'D_'.
9. Los campos de auditoria deben tener el prefijo AUD_

pdt.: Proximante se subirá un esquema de base de datos de ejemplo.